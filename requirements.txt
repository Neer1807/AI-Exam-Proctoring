Django==5.2.14
dj-database-url==2.2.0
django-cloudinary-storage==0.3.0
cloudinary==1.41.0
whitenoise==6.8.2
numpy==2.4.4
# NOTE: pinned to a wheel-available NumPy for cp314 (Windows) to avoid building from source.


opencv-python==4.11.0.86
mediapipe==0.10.35

Pillow==12.2.0

pytz==2024.2
ultralytics==8.3.62
ultralytics-thop==2.0.13

# Database (use binary wheels to avoid build tools)
psycopg[binary]==3.2.10

# Audio: use pure-python wheel alternative if you add it later.
# (Removed PyAudio because it commonly requires native build tools.)

# Misc minimal runtime
protobuf==4.25.5
requests==2.32.3
six==1.17.0
# xhtml2pdf removed because python-bidi builds from source on some Windows setups.
# PDF generation endpoints handle missing dependency gracefully.
# xhtml2pdf==0.2.17



# Production WSGI server
gunicorn==23.0.0
